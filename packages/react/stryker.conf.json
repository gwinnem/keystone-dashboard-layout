{
  "$schema": "https://raw.githubusercontent.com/stryker-mutator/stryker-js/master/packages/api/schema/stryker-schema.json",
  "_comment_known_limitation": "PAUSED, not resolved — a real run against this config crashed partway through the initial dry run with '[vitest-pool]: Failed to start threads worker' across many test files simultaneously, cause not yet definitively traced. Worth flagging: this package's own vitest.config.ts uses the identical '@/core' alias (resolving to the sibling packages/core/src) that was CONFIRMED to be the root cause of an equivalent, more clearly-diagnosed failure in the Vue package's own stryker.conf.json (see that file's own '_comment_known_limitation') — Stryker's default sandboxing only copies the current project directory into its temp sandbox, with no awareness that a path alias reaches outside it, so packages/core would be absent from this package's own sandbox too. Whether that's actually this specific crash's own root cause is unconfirmed (the failure mode looked different — a worker-thread startup crash, not an explicit 'failed to resolve import' error — and a competing theory, Windows long-path limits inside the deeply-nested .stryker-tmp sandbox path, was also raised but not verified either). Deliberately paused rather than guessed at further; revisit this comment, and Vue's own equivalent one, together before re-attempting either.",
  "packageManager": "pnpm",
  "testRunner": "vitest",
  "plugins": ["@stryker-mutator/vitest-runner"],
  "reporters": ["clear-text", "progress", "html", "json"],
  "clearTextReporter": {
    "allowColor": true,
    "allowEmojis": true,
    "logTests": false,
    "maxTestsToLog": 3,
    "reportTests": true,
    "reportMutants": true,
    "reportScoreTable": true,
    "skipFull": false
  },
  "htmlReporter": {
    "fileName": "reports/mutation/index.html"
  },
  "jsonReporter": {
    "fileName": "reports/mutation/mutation.json"
  },
  "coverageAnalysis": "perTest",
  "timeoutMS": 15000,
  "mutate": [
    "src/components/Grid/GridLayout.tsx",
    "src/components/Grid/GridItem.tsx",
    "src/components/Grid/hooks/*.ts",
    "src/hooks/*.ts",
    "!src/**/*.d.ts"
  ],
  "ignorePatterns": [
    "e2e",
    "e2e-fixture",
    "dist",
    "docs",
    "coverage"
  ]
}
