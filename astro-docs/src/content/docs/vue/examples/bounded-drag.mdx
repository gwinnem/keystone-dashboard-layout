---
title: Bounded drag to container
description: With isBounded enabled, items can't be dragged past the edges of the grid container.
---
import BoundedDrag from '../../../../components/examples/02-bounded-drag.vue';
import BoundedDragSource from '../../../../components/examples/02-bounded-drag.vue?raw';
import ExampleTryIt from '../../../../components/harness/ExampleTryIt.astro';

<ExampleTryIt code={BoundedDragSource} filename="bounded-drag.vue">
  <BoundedDrag client:visible />
</ExampleTryIt>

:::tip[Per-item override]
`isBounded` can also be set on an individual `GridItem` (its own
`is-bounded` prop) to bound just that item regardless of the layout's
default — leave it `null` (the default) to inherit from `GridLayout`.
:::
