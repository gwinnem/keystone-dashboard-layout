---
title: Prevent collision
description: Blocks a drag/resize from overlapping another item entirely, rather than pushing it out of the way.
---
import PreventCollision from '../../../../components/examples/08-prevent-collision.vue';
import PreventCollisionSource from '../../../../components/examples/08-prevent-collision.vue?raw';
import ExampleTryIt from '../../../../components/harness/ExampleTryIt.astro';

<ExampleTryIt code={PreventCollisionSource} filename="prevent-collision.vue">
  <PreventCollision client:visible />
</ExampleTryIt>

:::caution[Interacts with compaction]
`preventCollision` stops an item from overlapping another — it doesn't
disable compaction. If you want items to stay exactly where they're put
with no automatic repositioning at all, also set `:compact-type="ECompactType.NONE"`.
:::
