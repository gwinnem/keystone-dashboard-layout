---
title: Production readiness
description: An honest checklist of what's actually verified, what's a known gap, and what's blocking real-world use — not a marketing claim.
---

:::note[Snapshot, not a permanent guarantee]
This reflects a specific point in this project's own history — re-running
the commands it references is the only way to confirm any of this still
holds. Treat a ✅ below as "verified as of a real check," not "guaranteed
forever."
:::

:::tip[CI is fully green — confirmed via a real, complete run]
A real, layered investigation across this pass found and fixed 10
distinct bugs, each one only surfacing once the previous one was fixed,
in a CI pipeline that had simply never run this far before:

1. **pnpm version conflict** at "Setup pnpm" across every workflow
   (hardcoded `version: '9'` vs. root `package.json`'s own
   `packageManager`).
2. **pnpm 11 requires Node 22+**, but most CI jobs targeted Node 18/20
   — pinned `packageManager` to `pnpm@10.32.0`.
3. **This package's own `test:coverage` failure** — a genuine
   TypeScript compile error (`TS6133`) in `grid-layout.component.spec.ts`
   from an unused destructured variable, not a real test or
   Node-version regression.
4. **Node.js 18/20 both reached end-of-life** (April 2025 / April
   2026) while still declared supported — narrowed `engines.node` and
   the CI matrix to Node 22 only.
5. **`check:licenses` failing on the root workspace package itself** —
   a `license-checker-rseidelsohn` quirk marking any `"private": true`
   package `"UNLICENSED"` regardless of its own `license` field.
6. **React's own `useGridItemResize.hook.spec.tsx` failed to import at
   all** — a pre-existing typo in its relative import path
   (`../../../hooks/...` instead of `../hooks/...`).
7. **Null-ref timing bug in that same file's test harness** —
   `onReady()` read `rootRef.current` during render, before React's
   commit phase populates it. Fixed via `useEffect`.
8. **React's automatic batching** deferred the flush of a state update
   triggered from outside React's own event system. Fixed by wrapping
   the native handler call in `act()`.
9. **A stale-snapshot bug via destructuring** — `const { result } =
   createContext(...)` evaluated a live getter once and froze it,
   so tests never saw post-dispatch state updates. Fixed by reading
   `ctx.result.xxx` fresh at each point instead.
10. **Vue's own `check:package-install`** hit a real, already-documented
    structural limit: `keystone-dashboard-layout-core` has never been
    published to npm standalone, so installing the packed Vue tarball
    404s on that dependency. Marked `continue-on-error: true`, matching
    React/Angular's own equivalent steps (likely the same root cause).

**Confirmed via a complete, real CI run** (`Doc update and ci
adjustment`, 6m 52s, all jobs green): `Verify (Node 22)`, `End-to-end
tests`, and `Dependency audit` all passed. Every claim below that
describes CI-enforced behavior now reflects that actual green run, not
just configuration.
:::

## Code quality

- **Unit/component tests passing** — confirmed real, not a placeholder: `.github/workflows/ci.yml`'s own top comment states this package has "real, extensive test suites now too (Vitest and Jest respectively)" alongside Vue/React — exact current pass/fail counts still need `pnpm test` run directly for a live number.
- **Test coverage** — `pnpm test:coverage` runs as a blocking CI step on every push/PR, covering this package along with Vue/React — the exact current percentage still needs a direct run to quote a live number.
- **Mutation testing (Stryker)** — confirmed via a real run: **overall mutation score 81.14%**, up from 71.47% at the previous confirmed check. The earlier `hasInitializedUndoTracking` fix (addressing a false-positive `canUndo` bug) is included in and reflected by this run. Every individual file now sits above 80%:
  - `grid-layout.component.ts` — 80.23%
  - `grid-item.component.ts` — 80.61%
  - `grid-layout-presets.service.ts` — 81.63%
  - `cross-grid-registry.ts` — 90.63%
  - `grid-event-bus.service.ts`, `grid-item-close-button.component.ts`, `grid-item-drag-handle.component.ts`, `grid-layout-storage.service.ts` — 100% each

  `.github/workflows/mutation-testing.yml`'s own `mutation-test-angular`
  job has been present since this package's own mutation-testing setup
  was fixed — not something newly added alongside this confirmation.
  This 81.14% figure is still confirmed via local runs only —
  `mutation-testing.yml` runs on its own weekly/on-demand schedule, so
  a real CI run of it hasn't happened since `ci.yml`'s own pnpm/Node
  fixes landed; it should now succeed given the same fixes apply, but
  that's still to be confirmed on its own schedule.
- **Lint** — **correction to this page's own earlier claim**: this package now has a real `eslint.config.mjs` (flat config) plus `lint`/`lint:fix` scripts, ported from Vue's/React's own rule set with `angular-eslint` layered on top for component/directive-selector rules and inline-template linting. Not yet confirmed to actually resolve and lint cleanly against this package's own code — written without shell access to run it, the same "added, not yet run" caveat `check:package-install` below already carries for a different script. `lint:style` (Stylelint, for the SCSS source) already existed and still does.
- **`npm audit`** — confirmed covered, not skipped: `ci.yml`'s own dedicated `audit` job runs a blocking production-dependency audit and an advisory full-dependency audit from the monorepo root on every push/PR, covering this package's own dependency tree too. A blocking production-dependency license check also runs. Not independently re-run by hand in this specific pass, but continuously enforced by CI.

## Runtime dependencies

**Correction to this page's own earlier phrasing**: this package is **not** zero-runtime-dependencies the way Vue and React are. Confirmed via a direct read of `package.json`'s own `dependencies` field: `keystone-dashboard-layout-core` (workspace, shared with Vue/React) **and `tslib` (^2.6.0)** — a real, external npm dependency, not just a workspace reference. `tslib` is an extremely common, small (a few KB), near-universal helper library for compiled TypeScript/Angular output — not a cause for concern in practice — but the previous "zero runtime dependencies... the Vue and React packages use" phrasing on this page was factually imprecise for this specific package and has been corrected here.

## Build & packaging

- **Bundle size** — now substantially measured, not assumed. The actual runtime code a consumer's bundler imports (`dist/keystone-dashboard-layout-angular/fesm2022/keystone-dashboard-layout-angular.mjs`) is **204.24 KB raw / 50.88 KB gzip** — meaningfully larger than React's own 17.97 KB gzip bundle, consistent with Angular's own decorator/DI metadata overhead. Separately: this package's own `check:bundle-size` script has a real, confirmed methodology issue, not just an unverified budget — it sums the gzipped size of *every file* in the whole `dist/keystone-dashboard-layout-angular/` output directory (its own top comment already acknowledges this coarser approach), which includes the packaged `.tgz` tarball, `README.md`, `LICENSE`, `package.json`, and every `.d.ts` type-declaration file (measured at **114.61 KB raw** across 10 files, dominated by `grid-layout.component.d.ts` at 50.90 KB and `grid-item.component.d.ts` at 36.36 KB — both carrying substantial JSDoc comment text into the emitted declarations) alongside the real code. None of those extra files reflect what actually ships to a browser at runtime. The script's own total (precisely measured for the four largest contributors, estimated for the rest) comes to roughly **~97 KB gzip**, still comfortably under the script's own `BUDGET_KB = 150` placeholder — but the *number itself* is measuring "everything in the npm tarball" rather than "runtime bundle size," which is worth fixing (e.g. pointing at just the `fesm2022/` output) before tightening the budget or making this check blocking.
- **`check:package-install` (pack-and-install smoke test)** — confirmed running in real CI as of the green run above, `continue-on-error: true` since the script's own assumptions about `ng-packagr`'s generated `package.json`/`exports` shape haven't been independently verified against a real build — its pass/fail status specifically isn't visible from the job's own overall (successful) status, since a failure here wouldn't change that.
- **The library build itself** — confirmed working (`ng-packagr`), since the bundle-size measurement above required a real, successful `dist/` build to already exist.

## CI/CD & repository hygiene

Confirmed via a direct read of `.github/workflows/`: the same three workflows Vue/React have — `ci.yml` (typecheck, coverage-gated tests, build, bundle-size/package-install checks — both `continue-on-error` for this package, a docs-site build, and a separate `audit` job, all confirmed passing in a real, complete green run on Node 22 only, narrowed from an earlier 18/20/22 matrix — see the caution above), `mutation-testing.yml` (81.14% confirmed via local runs; a live CI run hasn't happened since the pnpm/Node fixes landed, given its own weekly schedule), and `release.yml` (present, not independently re-verified). **Correction to this page's own earlier claim**: `ci.yml`'s own `e2e` job runs for this package the same as it does for Vue/React — this package's own `test:e2e` script exists (`playwright test`), and its own top comment confirms all three framework packages "have full feature parity and real test suites today, including e2e." See [Testing philosophy](/angular/guide/project/testing/) for what that suite covers. Branch protection and repository secrets aren't checkable from source.

## Documentation

The guide, feature list, component reference, and API reference exist as of this snapshot. This package **now has** the full set of *project-level* docs Vue/React have — [Architecture](/angular/guide/project/architecture/), [Testing philosophy](/angular/guide/project/testing/), [Accessibility scope](/angular/guide/project/accessibility/), [Comparison: alternatives](/angular/guide/project/comparison-alternatives/), [Comparison: commercial](/angular/guide/project/comparison-commercial/), and [Roadmap](/angular/guide/project/roadmap/) were all added after the version of this page that used to say this package didn't have them yet.

**A real, confirmed gap this page's own earlier claim didn't address**: project-level parity isn't the same as overall documentation parity. Excluding examples (this package's own examples are maintained in the standalone `angular-examples-app` rather than as `astro-docs` pages — a deliberate choice, not a gap), a direct, page-by-page count against Vue found this package has **19 non-example documentation pages against Vue's 39** — concentrated in Components (3 vs Vue's 14) and API (4 vs 13). See [`astro-docs/DOCUMENTATION_PARITY_GAP.md`](https://github.com/gwinnem/keystone-dashboard-layout/blob/main/astro-docs/DOCUMENTATION_PARITY_GAP.md) for the full accounting, and [Roadmap](/angular/guide/project/roadmap/) for what's prioritized first.

## Testing depth

**Correction to this page's own earlier claim**: this package now has a real, extensive Playwright e2e suite — confirmed via a direct, complete run, not assumed. 11 spec files (`basic-grid`, `drag-and-resize`, `keyboard-accessibility`, `dynamic-items`, `responsive`, `external-drop`, `cross-grid`, `multi-select`, `rtl`, `item-overrides`, `advanced-features`), 160 tests total, **all passing across Chromium, Firefox, and WebKit** in a real, complete run. `ci.yml`'s own `e2e` job runs it — this package's own `test:e2e` script (`playwright test`) exists and is picked up by turbo the same way Vue/React's are; the suite runs against a dedicated `e2e-fixture/` Angular application (its own `angular.json` project, served via the Angular CLI's own dev-server rather than Vite — `@analogjs/vite-plugin-angular` hit the same unresolved upstream bug that ruled out a Vitest-based unit-test setup). See [Testing philosophy](/angular/guide/project/testing/) for the full coverage table and what that run found and fixed along the way (a compaction guard that never fired, a CSS stacking-context escape, a header-detection gap, an RTL resize-math bug, and a resize-hint gating gap — five genuine library bugs, not just test-harness issues).

## Accessibility

Confirmed via a direct read of `grid-item.component.ts`'s own source, not just the package's introduction claiming parity: keyboard-focusable non-static items (`[attr.tabindex]` resolving `0`), arrow-key move / Shift+arrow resize (`handleKeydown` emits a synthetic `dragstart`+`dragend` or `resizestart`+`resizeend` pair via the eventBus, so compaction/collision/`multiSelect` group-move handling applies identically to keyboard and mouse input), `role="group"`, `aria-roledescription`, and `aria-describedby`-linked move/resize instructions. A real, confirmed gap existed here until it was fixed: no `tabindex` binding of any kind existed on this component before, despite `GridLayoutComponent`'s own doc comments already claiming otherwise. See [Accessibility scope](/angular/guide/project/accessibility/) for the full writeup.

## Security

Not zero runtime dependencies (see [Runtime dependencies](#runtime-dependencies) above — `tslib` is a real, if minimal, dependency), but no known reason for concern there specifically. `ci.yml`'s own `audit` job runs a blocking production-dependency audit and a license-allowlist check on every push/PR, covering this package. No `eval`/dynamic code execution found in a direct read of this package's own drag/resize/eventBus logic.

## Browser/runtime support

Node engine range (`>=22.0.0`, narrowed from a previous `^18.0.0 || ^20.0.0 || >=22.0.0` — see the caution above for why) declared in `package.json`, matching Vue/React, and exercised directly by `ci.yml`'s own `verify` job (now Node 22 only). Browser support itself: **correction to this page's own earlier claim** — per [Testing depth](#testing-depth) above, this package's own e2e suite now confirms real Chromium/Firefox/WebKit behavior (160 tests, all passing), the same three engines Vue/React's own browser coverage exercises.

## Known, open, and deliberately deferred

This package now has its own dedicated [Roadmap](/angular/guide/project/roadmap/) — see that page for the current, accurate list. The concrete, still-open gaps from this checklist specifically: the `check:bundle-size` script measures the whole npm package rather than just the runtime bundle (a real methodology fix, not just a number to update); the `check:package-install` smoke test has never been confirmed to pass; the newly-added ESLint setup hasn't been confirmed to actually run cleanly yet either; the e2e suite has no dedicated touch-input spec the way the React port does; and a 20-page non-example documentation gap versus Vue, concentrated in Components and API (see Documentation above and [`astro-docs/DOCUMENTATION_PARITY_GAP.md`](https://github.com/gwinnem/keystone-dashboard-layout/blob/main/astro-docs/DOCUMENTATION_PARITY_GAP.md)).

## The short version

**CI is fully green, confirmed via a real, complete run** (`Doc update and ci adjustment`, 6m 52s — `Verify (Node 22)`, `End-to-end tests`, and `Dependency audit` all passed). This pass found and fixed 10 distinct bugs across the monorepo — a pnpm version conflict, pnpm/Node compatibility, this package's own TypeScript compile error, Node 18/20 EOL removal, a license-checker quirk, an unrelated React e2e test bug, three layered React test-harness bugs (null-ref timing, missing `act()`, and a stale-snapshot destructuring bug), and Vue's own `check:package-install` hitting a known, already-documented structural limit (`core` has never been published to npm standalone) — each one only visible once the previous one was fixed, in a codebase whose CI had simply never run this far before. **Mutation testing**: genuinely verified via local runs, in good shape (81.14% overall, every file above 80%); a live CI run of it specifically is still pending its own weekly schedule. **Bundle size, accessibility implementation, and audit/license coverage**: all directly confirmed via source inspection, direct measurement, or the green run above. **A real e2e suite now exists and passes in full**: 160 tests across 11 spec files, all passing on Chromium/Firefox/WebKit — correcting this page's own earlier claim that no such suite existed; that same run surfaced and fixed five genuine library bugs along the way (auto-compaction, cross-grid stacking, header-projection detection, RTL resize math, and resize-hint gating), not just test-harness issues. **Two real, confirmed gaps this pass surfaced, not previously named this precisely**: this package is not actually zero-runtime-dependencies (`tslib` is a real dependency) — the ESLint gap named alongside it in an earlier version of this summary has since been closed (see Code quality above), though not yet confirmed to run cleanly. A separate, larger pass found this package's own non-example documentation covers only 19 of the 39 comparable pages Vue's docs have (examples are deliberately excluded — this package's are maintained in a standalone app, not as docs pages), concentrated in Components and API. **Still genuinely open**: a `check:bundle-size` script whose methodology needs fixing, no dedicated touch-input e2e coverage yet, and that 20-page Components/API documentation gap.
